//! Nothing lives here: this crate exists so the workspace-level gates in
//! `tests/` have a member to ride — the examples' document-language
//! property spans every connector, and a `tests/` directory needs a
//! crate. See `tests/examples.rs` and `tests/workspace_rules.rs`.
